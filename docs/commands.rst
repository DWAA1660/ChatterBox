==================
Command line tools
==================

ChatterBox comes with a few command line tools that can help

Get the installed ChatterBox version
====================================

If have ChatterBox installed and you want to check what version
you have then you can run the following command.

.. code-block:: bash

   python -m chatterbox --version
